pipeline {
    agent any
    stages {
        stage('Start') {
            steps {                
                echo "Start"
            }
        }
    
        stage('Test SSH') {
            steps {
                sh "sudo ssh -i /home/ec2-user/pushdockerhub/DevOpspem.pem ec2-user@18.139.0.248"
                echo "SSH Thanh cong"
            }
        }
    }
}
