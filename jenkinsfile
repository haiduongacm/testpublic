pipeline {  environment {
    registry = "haiduongmilan/abc"
    registryCredential = 'todockerhub'
  }  agent any  stages {
    stage('Building image') {
      steps{
        script {
          docker.build registry + ":$BUILD_NUMBER"
        }
      }
    }
  }
}
