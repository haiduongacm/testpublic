stage("Docker push") {
    environment {
        DOCKER_USERNAME = credentials("haiduongmilan")
        DOCKER_PASSWORD = credentials("Kenko@123")
     }
     steps {
        sh "docker login --username ${DOCKER_USERNAME} --password ${DPKCER_PASSWORD}"
        sh "docker push haiduongmilan/test"
      }
}
     
