pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                sh "sudo ssh -i DevOpspem.pem ec2-user@18.139.0.248"
                echo "SSH Thanh cong"
            }
        }
    }
}
